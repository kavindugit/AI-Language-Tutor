# Server
PORT=4000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173

# Database
MONGODB_URI=mongodb://localhost:27017/ai_tutor

# Auth
JWT_SECRET=replace_me_with_strong_secret

# Mail (optional)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SENDER_EMAIL=

# AI services (Python microservices)
SPEECH_VISION_URL=http://localhost:8001

# Build metadata (optional, useful for /version endpoint)
APP_VERSION=0.1.0
GIT_COMMIT=dev
BUILD_TIME=
